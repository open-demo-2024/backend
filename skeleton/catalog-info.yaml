apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}${{ values.owner }}
  description: |
    Fruit backend component.
  annotations:
    github.com/project-slug: open-demo-2024/${{ values.name }}
    backstage.io/techdocs-ref: dir:.
    quay.io/repository-slug: rh_ee_symartin/${{ values.name }}
spec:
  type: library
  owner: user:smartinus44
  lifecycle: experimental
